# 📖 AI 摄影管家 · 使用说明

> 面向所有用户的完整使用指南。如果你只是想快速体验，看「快速开始」即可。

---

## 1. 这是什么？

**AI 摄影管家** 是一款**多平台**应用（Windows / macOS 桌面 + 手机版），能帮你从成千上万张照片中**自动挑出真正的好照片**，并为每张照片给出专业的摄影评分、摄影师点评和后期修图建议。

它由两部分组成：

- **本地预处理（免费）**：去重、相似照片检测、模糊/过曝/欠曝/分辨率过低过滤，全部在本机完成，不消耗任何 API 费用。
- **AI 视觉分析（需调用大模型 API）**：对选出的照片进行 8 维度评分、点评和后期建议。你只需申请一个支持视觉的模型 API Key，自行承担调用费用。

> 三种使用方式功能完全一致，共享同一套核心逻辑：
> - **桌面版**（Windows / macOS）：完整 GUI，适合处理电脑里的大批量照片（含 RAW）。
> - **手机版**：手机浏览器访问，电脑作后端，适合在手机上随手选照片分析。

---

## 2. 系统要求

### 桌面版

| 项目 | 要求 |
| --- | --- |
| 系统 | Windows 10 / 11（64 位）**或 macOS 11+** |
| Python | 3.13+（仅从源码运行时需要） |
| 磁盘 | 至少 1 GB 可用空间 |
| 网络 | 需要联网调用视觉模型 API |

### 手机版

| 项目 | 要求 |
| --- | --- |
| 后端电脑 | Windows / macOS（装好 Python 3.13+ 与项目依赖） |
| 手机 | 任意系统，自带浏览器即可 |
| 网络 | 手机与后端电脑在**同一 Wi-Fi 局域网** |

---

## 3. 快速开始

### 3.1 方式一：从源码运行（推荐给开发者 / 想二次开发的人）

```bash
# 克隆仓库
git clone https://github.com/laoziocean-code/ai-photo-manager.git
cd ai-photo-manager

# 创建虚拟环境并安装依赖
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# 启动
python -m src.main
```

### 3.2 方式二：自己打包成单文件 EXE（推荐给想免装 Python 的普通用户）

```bash
pip install pyinstaller
python build.py
```

打包完成后，双击 `dist/AI摄影管家.exe` 即可运行。

> 由于 GitHub 仓库有 100 MB 单文件限制、且 EXE 体积较大（150–500 MB），发布版 EXE 不会上传到仓库，请按上面步骤自行打包，或使用作者在发行页（Release）提供的安装包（如有）。

### 3.3 方式三：macOS 桌面版

```bash
git clone https://github.com/laoziocean-code/ai-photo-manager.git
cd ai-photo-manager
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt        # rawpy / PySide6 / opencv 均有 macOS 预编译 wheel
python -m src.main
```

打包为 `.app`（可选）：

```bash
pip install pyinstaller
python build_mac.py
```

产物 `dist/AI摄影管家.app`，双击运行。**首次打开若提示「无法验证开发者或包含恶意软件」**：在「访达」中右键 `AI摄影管家.app` →「打开」→「仍要打开」；或在「系统设置 → 隐私与安全性」点击「仍要打开」。如需分发给他人，建议用 Apple Developer 证书签名并公证。

### 3.4 方式四：手机版（浏览器访问）

手机版以「手机浏览器 + 电脑后端」方式运行，后端复用桌面版全部核心逻辑。手机与电脑需在同一 Wi-Fi。

1. 在电脑上（Windows / macOS 均可）安装依赖并启动后端：

   ```bash
   pip install -r requirements.txt        # 已含 flask
   python mobile/run_mobile.py
   ```

2. 启动后会打印访问地址，例如：

   ```
   手机访问：http://192.168.1.10:8778
   ```

3. 用手机浏览器打开该地址，按页面指引：配置 API Key → 选照片上传 → 开始分析 → 查看结果与报告。

> 手机版后端默认监听 `0.0.0.0:8778`，可用 `--port` 改端口。详细操作见第 10 节。

---

## 4. 首次配置：API Key 与模型

AI 评分需要调用视觉大模型。**最简单的是用智谱 GLM（国内直连、注册即有免费额度）**。

### 4.1 获取智谱 API Key

1. 打开 <https://open.bigmodel.cn>（智谱开放平台），注册/登录账号。
2. 进入「API Keys」页面，点击「创建 API Key」，复制生成的 Key（形如 `xxxxxxxx.xxxxxxxx`）。
3. 说明：`glm-4v-plus` 为收费旗舰模型；`glm-4v-flash` 为免费模型。可用额度以平台显示为准。

### 4.2 在软件中填写

1. 启动应用，在首页顶部选择模型预设，默认是 **GLM-V4（智谱视觉）**。
2. 粘贴你的 API Key。
3.（可选）在「模型名（可覆盖）」中填写具体模型 id，例如 `glm-4v-flash` 可省钱。

> 也支持其他模型：OpenAI GPT、Google Gemini、Anthropic Claude，以及任意 OpenAI 兼容端点（需填 base_url）。Key 仅本地加密保存，不会上传到任何服务器。

---

## 5. 开始使用

### 5.1 选择照片文件夹

- 点击「选择文件夹」，或将文件夹**直接拖拽**到窗口中央区域。
- 支持格式：JPG / PNG / JPEG / TIFF / BMP / WEBP，以及**所有主流相机 RAW**（NEF / NRW / CRW / CR2 / CR3 / ARW / SR2 / SRW / DNG / RW2 / RWL / RAF / ORF / PEF / 3FR / FFF / IIQ / MOS / MEF / MRW / ERF / KDC / DCR / X3F / BAY 等）。RAW 通过 rawpy（LibRaw）解码，并带文件头魔数兜底——即使扩展名被改名也能识别并尝试解码。

### 5.2 设置参数

- **精选数量（Top-N）**：范围 3–50，可直接用键盘输入数字，或用右侧上下箭头调整。例如填 20，表示最终挑出最好的 20 张。
- **去重档位**：选择相似照片的剔除强度：
  - **标准**：只去掉几乎完全相同的重复照片（推荐）。
  - **严格**：连高度相似的照片也去掉。

### 5.3 开始分析

点击「开始分析」：

1. **本地预处理**：先在本机做去重、清晰度/曝光度检查（进度条快速推进，不花钱）。
   - **RAW 文件的特殊规则**：NEF/CR2/ARW 等 RAW 保留完整动态范围，欠曝/夜景可后期恢复，且全局清晰度/曝光启发式对暗场景（如夜空大片纯黑）会系统性误判——因此 **RAW 只做去重与分辨率校验，不做模糊/曝光淘汰**，质量交给 AI 视觉评分判断。普通 JPG/PNG 仍按原规则过滤模糊/严重过曝欠曝/分辨率过低的照片。
2. **AI 视觉评分**：逐张调用视觉模型，给出 8 维评分与点评。
3. 窗口会显示**总进度和预计剩余时间（ETA）**，大目录也不用干等。

### 5.4 查看与导出

- 分析过程中可切到「分析报告」页实时查看结果。
- 分析完成后：
  - 输出目录下会自动生成 **`AI精选/`** 文件夹，里面是按分数排序的最好照片，每张带 `推荐理由.txt` 说明。
  - 生成一份**杂志风格的 HTML 摄影报告**，可直接用浏览器打开、截图或分享。
- 历史分析记录会保存在「历史」中，方便回看。

---

## 6. 功能详解

### 8 维评分

| 维度 | 含义 |
| --- | --- |
| 构图 | 画面布局是否平衡、有张力 |
| 光影 | 曝光、明暗对比、光线方向 |
| 色彩 | 色调、饱和度、白平衡 |
| 清晰度 | 对焦、锐度、噪点控制 |
| 主体 | 主体是否突出、明确 |
| 情绪 | 画面传递的情绪感染力 |
| 故事 | 照片是否讲了一个故事 |
| 发布价值 | 适合发布到社交媒体的程度 |

### Lightroom 后期建议

对每张精选照片给出：**可调参数**（曝光、对比、HSL 等）+ **风格方向**（如“增加暗角突出主体”）+ **为什么这么调** 的解释。

### 完成提醒与自动关机

- 可勾选「分析完成时提醒」，结束后弹出系统通知。
- 可勾选「分析结束自动关机」，适合睡觉前挂着跑大目录。

---

## 7. 常见问题（FAQ）

**Q1：RAW 文件打不开？**
本项目支持**所有主流相机 RAW 格式**（基于 rawpy / LibRaw）。若打不开：
- 请确认 `rawpy` 已安装（`pip install rawpy`）。Windows 打包版已内置 `raw_r.dll`，macOS 打包版已收集 libraw 动态库。
- 桌面版支持处理电脑里的 RAW 文件；手机版因手机浏览器限制，主要处理 JPG/PNG/WEBP（手机相册常见格式），RAW 建议用桌面版处理。
- 若 RAW 文件扩展名被改名（如 `.nef` 改成 `.bin`），程序会按文件头魔数自动识别并尝试用 rawpy 解码。

**Q2：提示 API Key 无效 / 401？**
检查 Key 是否复制完整、是否在平台开通了该模型；智谱免费模型请用 `glm-4v-flash`。

**Q3：分析速度很慢？**
速度取决于模型和网络。想快可：用 `glm-4v-flash`（免费且快）、把「去重档位」调高减少送入 AI 的张数、先处理子目录。

**Q4：要花多少钱？**
本地预处理免费；AI 部分按你所用模型的官方计费，费用由你自己控制（精选数量越小、用免费模型，成本越低）。

**Q5：EXE 体积为什么这么大？**
因为内置了 OpenCV、PySide6、rawpy 等完整运行库，属于单文件打包的正常体积。

---

## 手机版使用详解

手机版让你**在手机上选照片、看结果**，而所有处理（预处理 + AI）都在你自己的电脑上完成——无需把照片传到陌生服务器，也不用在手机上装任何 App。

### 启动后端

在电脑上（Windows / macOS 均可）：

```bash
pip install -r requirements.txt        # 已含 flask
python mobile/run_mobile.py
```

启动后控制台会打印：

```
========================================================
  📸 AI摄影管家 · 手机版
========================================================
  电脑访问：http://127.0.0.1:8778
  手机访问：http://192.168.1.10:8778
  （手机与电脑需在同一 Wi-Fi 局域网）
--------------------------------------------------------
  按 Ctrl+C 停止服务。
========================================================
```

### 手机操作流程

1. **配置**：手机浏览器打开地址 → 选择模型预设 → 粘贴 API Key（本地加密保存）→ 设置精选数量与去重档位 → 保存。
2. **上传**：点击「选择照片」多选手机里的照片 → 上传（照片通过局域网传到电脑）。
3. **分析**：点击「开始分析」，页面实时显示进度（本地预处理 → AI 评分 → 导出报告）。
4. **结果**：查看 AI 精选（含评分、点评、修图建议、发布建议、EXIF）、良好/普通列表、废片原因；可查看完整 HTML 报告、逐张下载精选原图。

### 常见问题

- **手机打不开地址？** 确认手机与电脑连同一个 Wi-Fi；关闭电脑防火墙对 8778 端口的拦截，或用 `--port` 换端口。
- **上传很慢？** 取决于照片大小和局域网速度；建议先用少量照片测试。
- **RAW 能用吗？** 手机版后端支持 RAW，但手机浏览器多选时一般只能选 JPG/PNG；RAW 文件建议用桌面版处理。
- **数据存哪？** 上传的照片与分析结果存在 `~/.ai_photo_manager/mobile/` 下；停止服务后仍在，可手动清理。

---

## 8. 隐私与安全

- **API Key 仅保存在你本机**，通过 cryptography 加密写入系统配置，不上传、不展示。
- 照片分析时会**把照片内容发送给你所选的模型服务商**（用于视觉理解）。在意隐私的照片请不要放入。
- **手机版**：照片通过你自己的局域网 Wi-Fi 传到你的电脑处理，不经过任何第三方服务器；分析时同样会发送给所选模型服务商。
- 请勿把 API Key 提交到公开仓库；本项目 `.gitignore` 已忽略常见配置文件。

---

## 9. 技术支持与反馈

- 发现 Bug、想提需求：欢迎在 GitHub 仓库提交 [Issue](https://github.com/laoziocean-code/ai-photo-manager/issues)。
- 觉得好用的话，点一个 ⭐ Star，就是对我最大的鼓励！

---

<div align="center">用 AI 摄影管家，让好照片自己浮现。📸</div>
